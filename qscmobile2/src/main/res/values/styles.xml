<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:textSize">17sp</item>
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="AppWidgetTimetableDateText">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>>
        <item name="android:textSize">14dp</item>
    </style>

    <style name="AppWidgetTimetableDateText.Active">
        <item name="android:textColor">#FFFFFFFF</item>
    </style>

    <style name="AppWidgetTimetableDateText.Fading">
        <item name="android:textColor">#BFFFFFFF</item>
    </style>

    <style name="AppWidgetTimetableTaskText"/>

    <style name="AppWidgetTimetableTaskText.Finished">
        <item name="android:textColor">@color/grey_light</item>
    </style>

    <style name="AppWidgetTimetableTaskText.Active">
        <item name="android:textColor">@color/blue_light</item>
    </style>

    <style name="AppWidgetTimetableTaskText.Scheduled">
        <item name="android:textColor">@color/grey_dark</item>
    </style>

</resources>
